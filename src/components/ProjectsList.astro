---
import ProjectCard from './ProjectCard.astro';

interface Props {
  projects: Array<{
    title: string;
    type: string;
    year: string;
    image?: string;
  }>;
  base: string;
}

const { projects, base } = Astro.props;
---

<div class="works-list">
  {projects.map((project) => (
    <ProjectCard project={project} view="list" base={base} />
  ))}
</div>
